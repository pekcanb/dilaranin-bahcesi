export type GardenFlower = {
  id: string;
  type: "daisy";
  slotIndex: number;
  x: number;
  y: number;
  scale: number;
  rotation: number;
  createdAt: string;
};

export type Screen = "home" | "exercise" | "completion";
